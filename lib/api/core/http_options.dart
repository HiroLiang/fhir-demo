class HttpOptions {
  static const Duration connectTimeout = Duration(milliseconds: 5000);
  static const Duration receiveTimeout = Duration(milliseconds: 5000);
  static const Duration sendTimeout = Duration(milliseconds: 5000);
}